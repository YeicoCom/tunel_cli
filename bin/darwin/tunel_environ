#!/bin/bash

HOME=$HOME
GO_VERSION=1.22.2
YEICO_LINK=$HOME/.asdf/installs/golang/$GO_VERSION/packages/bin/yeico_link
TUNEL_WS=wss://dash.tunel.mx/link/websocket
#wg-quick not found unless homebrew added to path
#wg-quick requires homebrew bash version 4
export PATH=/opt/homebrew/bin:$PATH
